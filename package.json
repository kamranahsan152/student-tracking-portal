{
  "name": "student-submission-portal",
  "private": true,
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build && npm --prefix server install",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "dependencies": {
    "dotenv": "^17.4.2",
    "googleapis": "^176.0.0"
  }
}
